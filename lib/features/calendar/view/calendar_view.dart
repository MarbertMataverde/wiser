import 'package:flutter/material.dart';

class WisedarView extends StatelessWidget {
  const WisedarView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('W I S E D A R'),
      ),
    );
  }
}
